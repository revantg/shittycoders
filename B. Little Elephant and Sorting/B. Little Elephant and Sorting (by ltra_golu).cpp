#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, mx = 0;
    vector<int> v(n);
    vector<int> ans(n);
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
        mx = max(mx, v[i]);
    }
    for (int i = 0)
}
